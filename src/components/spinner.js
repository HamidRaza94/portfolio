import PropTypes from 'prop-types';

const Spinner = () => {
  return (
    <div className={`border-t-primary border-solid animate-spin rounded-full border-white border-2 h-6 w-6`} />
  );
};

export default Spinner;
